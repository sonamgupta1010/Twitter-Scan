// Import React to use its features
import React from "react";

// Define the Options component
const Options = () => {
    return (
        <div>
            {/* Render an h1 element with Tailwind CSS classes */}
            <h1 className="text-4xl text-green-500">Hello World</h1>
        </div>
    );
};

// Export the Options component to make it available for other parts of the application
export default Options;
